# OpenTerminal — remote shell for AI features
# Start server: docker run -p 8000:8000 -e API_KEY=your-key ghcr.io/open-webui/open-terminal
# Or: pip install open-terminal && open-terminal run --port 8000 --api-key your-key
VITE_OPEN_TERMINAL_URL=http://localhost:8000
VITE_OPEN_TERMINAL_API_KEY=your-secret-key
